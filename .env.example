# TreeVault — Environment template (copy to .env at project root)
# Usage:
# 1) Copy this file to .env
# 2) Adjust values as needed
# 3) Run: docker-compose up -d

# Required by docker-compose (PostgreSQL + Backend)
DB_NAME=treevault
DB_USER=treevault
DB_PASSWORD=treevault

# Notes
# - The frontend inside Docker does NOT need configuration here.
# - For local frontend dev (Vite), set VITE_API_URL in your shell or create
#   treevault-frontend/.env.local with: VITE_API_URL=http://localhost:8080
#   IMPORTANT: do not append /api/v1 — the frontend already adds it to requests.
# - To run backend locally (outside Docker), set these in your terminal before starting:
#   SPRING_DATASOURCE_URL=jdbc:postgresql://localhost:5432/treevault
#   SPRING_DATASOURCE_USERNAME=treevault
#   SPRING_DATASOURCE_PASSWORD=treevault

# Optional (advanced): Backend Docker profile CORS overrides
# These are read by Spring if present in the container environment. To use via
# docker-compose, add the variables under services.backend.environment.
#CORS_ALLOWED_ORIGINS=http://localhost:3000
#CORS_ALLOWED_METHODS=GET,POST,PUT,DELETE,OPTIONS
#CORS_ALLOWED_HEADERS=Content-Type,Authorization,X-Requested-With
#CORS_MAX_AGE=3600
